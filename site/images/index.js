// Optional: Add JavaScript if you want additional interactivity
document.querySelectorAll('.photo').forEach(photo => {
    photo.addEventListener('click', () => {
      alert('You clicked on a photo!');
    });
  });
  